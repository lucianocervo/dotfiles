- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.dotfiles: '' (ter o nosso dotfiles fora da home directory)
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig: 
    ~/.config/bat:
      create: true
      path: config/bat

- create:
    - ~/Work
#    - ~/.downloads
#    - ~/.vim/undo-history

- shell:
  # - chsh -s $(which zsh)
  - 
     command: ./setup_homebrew.zsh
     stdin: true
     stdout: true
     stderr: true
